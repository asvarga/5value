
notes
-----

## indexing into an array with a variable int requires some magic:
def ind(vec, i):
	veci = Int("%s[%s]" % (NAME(vec), i))
	for j in range(len(vec)): 
		f.append(Implies(i == j, veci == vec[j]))
	return veci

- the real implementatino of ind handles arbitrary dimension matrices and non-variable indices

TODO
----

- memoize indexing to reduce repeat formulas